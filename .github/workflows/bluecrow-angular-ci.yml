name: Bluecrow Angular CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'libs/bluecrow-angular/**'

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Bluecrow Angular verification action
        uses: ./libs/bluecrow-angular
